// GENERADO por build.js a partir de la env var DB_SCHEMA de Vercel — no editar a mano en un deploy.
// En desarrollo local (sin build) se usa el valor committeado en este archivo.
// SIBEX UCV vive en su propio schema `sibex` (ver supabase/sibex-schema-
// install.sql), separado de `public` para no pisar otro sistema que
// comparta el mismo proyecto Supabase. Recordar setear DB_SCHEMA=sibex
// también como env var de Vercel para producción (ver build.js), y el
// secret DB_SCHEMA=sibex de la Edge Function manage-users.
export const DB_SCHEMA = "sibex";
