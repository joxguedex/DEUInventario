// GENERADO por build.js a partir de la env var DB_SCHEMA de Vercel — no editar a mano en un deploy.
// En desarrollo local (sin build) se usa el valor committeado en este archivo.
// El esquema nuevo vive directo en `public` (ver supabase/new-project-schema.sql) —
// ya no hace falta un esquema con nombre propio para convivir con AcopioUCV/UCVComandas.
export const DB_SCHEMA = "public";
